export const enum GamePhase {
    LOBBY = "lobby",
    WRITE_GOOD = "write good",
    RESCUE = "rescue",
    SWAP_CARDS = "swap cards",
    WRITE_BAD = "write bad",
    KILL = "kill",
    WRITE_MODIFIERS = "write modifiers",
    DECISION = "decision",
    SCORES = "scores",
}